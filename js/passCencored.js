/*password cencored*/

function check() {
    var pass = document.getElementById("password");
    if (pass.type === "password") {
        pass.type = "text";
    } else {
        pass.type = "password";
    }
}

/*password cencored for password*/

function checks() {
    var pass = document.getElementById("passwords");
    if (pass.type === "password") {
        pass.type = "text";
    } else {
        pass.type = "password";
    }
}


/*password cencored for confirm password*/

function checkses() {
    var pass = document.getElementById("passwordz");
    if (pass.type === "password") {
        pass.type = "text";
    } else {
        pass.type = "password";
    }
}